
{{ form_start(subscribe_form, {'action': path('subscribe_create')}) }}
{{ form_row(subscribe_form) }}
<button type="submit" class="btn btn-sm btn-info btn-block">Subscribe</button>
{{ form_end(subscribe_form) }}
