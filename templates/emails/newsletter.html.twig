
<div style="background: #80bdff;" align="center">
    <h2>Top rated posts for this moment:</h2>
</div>
<div style="background: #80bdff;">
    {%  for post in posts|rate %}
        <hr>
        <h3><a href="{{ url('post_show', {'slug': post.slug}) }}">{{ post.title }}</a></h3>
        <small><b>Created by:
            </b><a href="{{ url('user_show', {'id': post.user}) }}">{{ post.user.name }}</a></small>
        <small><b>Tags:</b>
            {% if post.tags is defined and post.tags is not empty %}
                {% for tag in post.tags %}
                    <a href="{{ url('tag_show', {'id': tag.id}) }}">{{ tag.name }}</a>
                {% endfor %}
            {% else %}
                <i>NoTagsAdded</i>
            {% endif %}
        </small>
        <br/>
        <div>{{ post.getBodyTruncated(120) | raw }}</div>
    {% endfor %}
</div>
<div style="background: #80bdff;" align="center">
    <p>Z-Blog@2019</p>
</div>
