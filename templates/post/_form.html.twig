{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.body) }}
    {{ form_row(form.user_id) }}
    {{ form_row(form.tags) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
